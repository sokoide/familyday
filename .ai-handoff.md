# AI Handoff — familyday(ドラゴン城の秘宝 2026)

最終更新: 2026-07-07
現在のブランチ: `main`(未プッシュ・origin/main より先行)

## 完了したこと

### stage 1〜4 に「初めからやり直す」ボタンを追加し、確認ダイアログ付きで最初画面へ戻れるようにした
- `2026/web/index.html`: stage の input-area に mic 左配置の `btn-stage-restart` を追加
- `2026/web/src/app/messages.ts`: `stage.restart` / `stage.restartConfirm` を日英で追加
- `2026/web/src/app/main.ts`: stage リセット用の共通経路を追加し、YES/OK 時に sessionStorage クリア + `newSessionId()` 再生成 + intro へ復帰
- `2026/web/src/app/main.ts`: `startAdventure()` で `phase: "stage"` を明示し、Good/Great 後の遷移が止まらないよう修正
- `2026/web/src/app/main.ts`: リセット時に `endingImage.onerror` を解除し、`src` は属性削除で安全にクリアするよう修正
- 同じ共通経路をエンディング画面の「もういちど」にも適用し、リセット処理を一本化
- リセット直後に古い判定結果やエンディング応答が混ざらないよう、`gameEpoch` で遅延応答を無効化
- `2026/web/src/style.css`: stage の mic 左にリセットボタンを並べる `stage-actions` を追加
- `npm run build` は成功確認済み

### ステージ/練習画面の画像と説明欄の比率を 75% / 25% に調整
- `2026/web/index.html`: `practice` と `stage` の本文を `stage-content` で包み、画像と説明文を横並びに再構成
- `2026/web/src/style.css`: `stage-content` を `flex-wrap: nowrap` にし、画像を `flex: 0 0 75%`、説明欄を `flex: 0 0 25%` に固定
- 画像が縮んでいた問題を解消し、説明欄を半分にしたい意図に合わせて比率を明確化
- `npm run build` は成功確認済み

### `b725fad` fix: READMEの画像参照をw.jpgに統一 + マイクボタンを角丸小サイズに変更
- **README.md**: 9箇所の古い画像参照(`s1.jpg`等)を `w.jpg`(`s1w.jpg`等)に修正。コード側は前コミットで対応済みだったがREADMEだけ更新漏れがあった
- **style.css**: マイクボタンを180px円形→180×90px角丸(`border-radius: 18px`)に縮小。アイコン64px→32px、画面に収まりやすく

### `0710447` change: jpg to 16:10 aspect ratio
- 画像を16:10アスペクト比に変更。ファイル名に `w` サフィックス追加(`s1w.jpg`等)
- `main.ts`/`handler.go`/`handler_test.go`/`integration_test.go` の参照を `w.jpg` に更新

### `ef75887` feat: エンディングに冒険履歴に基づく約400字の物語を生成・表示
- `prompts.go`: storySystemPrompt/storyPrompt を「1〜2文の短い要約」から「約300〜400字の具体的な冒険物語」に変更
- `index.html`: エンディング画面に `<p id="ending-story-text">` を追加
- `main.ts`: `renderEnding` で `res.story` を表示
- `style.css`: `.ending-story-text` スタイル追加

### `502a3c4` fix: ending-story要素不存在によるmain()例外で全ボタン無効化の回帰を修正
### `f201bf3` feat: タイムアップ時の専用メッセージ追加
### `2c7d8f5` fix: レビュー残り3件対応(History上限・タイムアップ統一・README)
### `d23cf76` Remove obsolete image generation paths

## 進行中の状態

- **ブランチ**: `main`（未プッシュ・`origin/main` より大幅先行）
- **未コミット変更**: あり（今回の stage リセット導線と既存の未反映UI調整）
- **APIキー**: `2026/.env` に設定済み（`source ~/.glm` で読込）
- **画像**: 全て `w.jpg`（16:10アスペクト比）。`web/public/images/` に s1w-s4w/successfulw/failedw/practicew.jpg を配置。古い `w` なしファイルも残存（実害なし・コードから未参照）
- **判定/ストーリー**: `gemini-3.1-flash-lite` で動作確認済み
- **画像生成**: 完全削除済み。固定JPGのみ
- **エンディング物語**: 冒険履歴をGeminiに渡し、約300〜400字の具体的物語を生成。画面とメール本文の両方に表示
- **マイクボタン**: 角丸小サイズ（180×90px）に変更済み

### 動作確認済み(2026-07-06)
- `npm run build`: 成功（28KB）
- `go build` / `go vet` / `go test ./...`: 全緑
- `gofmt -s`: クリーン

### API課金見積もり（100プレイ時）
- gemini-3.1-flash-lite単価: 入力$0.25/100万・出力$1.50/100万
- 100プレイ（50人×2回）: 入力40〜60万・出力8〜15万トークン
- 費用: **約$0.16〜0.30**（数十セント台・懸念なし）

## 次回の具体的なステップ

### 1. 実機UXの最終確認（最優先）
`make build && make run` → `http://localhost:8080/` で以下を確認:
- stage 1〜4 の「初めからやり直す」ボタンが mic の左に表示され、確認ダイアログが英日で切り替わるか
- YES/OK 後に intro へ戻り、履歴・タイマー・sessionId がリセットされるか
- Good/Great 後に次ステージへ進めるか、`phase: "stage"` の明示設定が効いているか
- リセット時に `endingImage` の `onerror` が残らず、空 src を読みにいかないか
- マイクボタンが角丸小サイズ（180×90px）で画面に収まるか
- intro画面で「れんしゅう」「ぼうけんをはじめる」の両ボタンが動作するか
- れんしゅう: practicew.jpg表示、「こうばんにとどける」→成功(緑)、「とどけない」→失敗(赤)
- 4ステージ(stage1=川→stage4=ドラゴン)が正しく進行
- ステージ画像が16:10で表示されるか
- エンディング物語が約300〜400字で生成され、プレイヤーの発言が組み込まれているか
- タイムアップ時マイクが「しゅうりょう」でdisabled、メッセージは「じかんが きたよ!」
- メール送信で冒険物語+画像URL+署名が入るか

### 2. 音声入力の確定
- macOS Edge で `[speech] error:` のコードをコンソール確認
- 会場は Windows 11 Edge。**Windows 実機でのマイク動作確認が必須**

### 3. 本番デプロイ
- 公開Linuxサーバへ `2026/` 配置 → `make build` / `make build-bin`
- `2026/.env` の `PUBLIC_BASE_URL` を設定
- nginx 等で HTTPS → `:8080`。systemd で常駐
- `static/images/` 配置を忘れずに（`w.jpg` ファイル群）

### 4. コミットのプッシュ
- `git push origin main`
- 追加された `2026/.gitignore` は `/web/public/sherpa/` を無視するためのもの

## よく使うコマンド

```bash
cd 2026
make                  # build + unit(既定・ネットワーク不要)
make run              # サーバ起動(2026/.env を読む)
make check            # Clean Architecture チェック
cd web && npm run build  # フロントのみビルド
```

## 設計上の判断点(記録)
- **4ステージ構成**: stage1=川、stage2=ゴーレム、stage3=炎、stage4=ドラゴン。`isLast`判定は `stageIndex >= 3`
- **れんしゅうモード**: 別Phase("practice")。フロント簡易キーワード判定でサーバー不要。否定形は失敗に倒す
- **画像は固定JPG**(LLM生成完全廃止)。16:10アスペクト比。`web/public/images/*w.jpg` に版管理
- **メールは `mailto:`**(画像添付不可)。本文に画像URLをテキストで記載。base URLは `window.__FD_IMAGE_BASE__` でコンフィグ可能
- **エンディング物語**: Gemini に `state.history`（冒険履歴）を渡し、プレイヤーの実際の発言を組み込んだ約300〜400字の物語を生成
- **エンディングメッセージ**: クリア時「けっか: クリア!」、ライフ0時「けっか: ざんねん!」、タイムアップ時「じかんが きたよ!」で区別
- **reason のライフ言及は `sanitizeReason`(`usecase/judge.go`)で最終防御**
- **UXは「行動」ベース**(「呪文」呼称は全廃)
- **エンディング到達時はマイクdisabled**。「しゅうりょう」ラベルで明示
- **表示領域は80vw**(上限1200px)
- **セキュリティ**: ボディ上限64KiB、History配列上限16件/各300ルーン、`crypto/rand`失敗時はフォールバック廃止
- 来年は `2026/` を `2027/` にコピーし、`domain/catalog.go` + `web/src/app/messages.ts` + `web/public/images/` を差し替えれば再利用可能
