# AI Handoff — familyday(ドラゴン城の秘宝 2026)

最終更新: 2026-07-08
現在のブランチ: `main`

## 完了したこと

### 練習画面の「もどる」ボタンをマイク左に配置(2026-07-08)

練習(`practice`)画面の戻るボタンを、ステージ画面の「初めからやり直す」と同じくマイクの**左**に配置。下部のボタンを削除し、`.stage-actions` でマイクと横並びに。確認ダイアログは**なし**(即座にイントロへ戻る)。ラベルを「さいしょにもどる」(英: `Back to start`)に変更。

- **HTML**(`web/index.html`): `btn-practice-back` を `.input-area` 外の最下部 → `.stage-actions` 内(マイクの前=左)へ移動。class を `big-btn btn-secondary` → `small-btn btn-reset-stage` に変更。`id` は維持
- **i18n**(`web/src/app/messages.ts`): `practice.back` を ja `もどる` → `さいしょにもどる` / en `Back` → `Back to start`
- **ハンドラ/ラベル設定**(`main.ts`): 変更不要(既存 `practiceEls.back` 参照と no-confirm イントロ戻り処理がそのまま動作)
- **CSS**: 変更不要(`.stage-actions` / `.btn-reset-stage` を再利用)
- `npm run build` 成功(28.60 KB)

### エンディングタイトルの欠損表示を修正(2026-07-08)

ステージ4終了後の結果画面で、日本語タイトルが `🏆 でんせつの...` / `✨ がんばった...` と途中切れで不自然に表示されていた問題を修正。`messages.ts` の `ending` ブロック4行から末尾 `...` を除去し、「ゆうしゃ」を補完。

| 対象 | 修正前 | 修正後 |
|------|--------|--------|
| `titles.great` | `🏆 でんせつの...` | `🏆 でんせつの ゆうしゃ エンド` |
| `titles.success` | `✨ がんばった...` | `✨ がんばった ゆうしゃ エンド` |
| `shortLabel.great` | `でんせつの...` | `でんせつの ゆうしゃ` |
| `shortLabel.success` | `がんばった...` | `がんばった ゆうしゃ` |

- 英語版・`gameover` 系は変更なし(欠損なし・現状維持)
- `titles` は `○○の ゆうしゃ エンド` 形式 → gameover の「バッドエンド」と統一
- `npm run build` 成功(28.59 KB)
- ファイル: `web/src/app/messages.ts:197-205`(日本語 `ending` ブロックのみ)

### レビューで発見した重要度[高]5件のバグを修正(2026-07-07)

3観点レビュー(Goバックエンド / フロントエンド / 正確性・セキュリティ)で検出した [高]5件をすべて修正。ユーザ精査で優先度を確定後、H1→H3→H4→H5→H2 の順に対応。修正後に自己レビューを実施し、H3 の抜け(復元パス onZero に `speech.stop()` が未追加)を発見・補修。

| ID | 内容 | 場所 |
|----|------|------|
| **H1** | History 切り詰めを先頭16件 → **末尾16件**に(最新の発言が物語に反映される) | `server/internal/adapters/presentation/http/handler.go:100` |
| **H2** | セッション復元時 `renderStage` 後に `isProcessing` を見て入力ロック維持(二重送信防止) | `web/src/app/main.ts:239` |
| **H3** | タイムアップ時に `speech.stop()` を追加(新規/復元の両onZero) + mic 完了後に `state.phase !== "stage"` ガード追加 + **`submitInput` 冒頭でphaseガード集約(手動入力経路も保護)** | `web/src/app/main.ts:248,362`(stop) / `:728`(mic phaseガード) / `:619`(submitInput集約防御) |
| **H4** | `startAdventure` の onTick に `timerRemaining` 保存追加(復元パスと対称化) | `web/src/app/main.ts:354-355` |
| **H5** | レートリミッタで `len(pruned)==0` の delete 分岐を冒頭に移動。枯渇キーが確実に除去される。回帰テスト2件追加 | `server/internal/adapters/infra/ratelimit/rate_limiter.go:35-41` / `rate_limiter_test.go` |

**H3 の修正は3点構成**(ユーザ精査+自己レビューで拡張):
1. `speech.stop()` を両onZeroに追加(新規 `:362` / 復元 `:248`)
2. mic 完了後(`:728`)の `state.phase !== "stage"` ガード(stop は resolve するため)
3. **`submitInput` 冒頭(`:619`)で phase ガードを集約** — 手動入力経路(manualBtn/keydown)が phase チェックなしに直接 `submitInput` を呼んでいたため、タイムアップ後に phase=ending でも API が飛ぶ可能性があった。onZero で manualBtn.disabled=true にする UI 防御はあるが、集約防御で保護

**H5 の回帰テスト**: `TestLimiter_ExhaustedKeyPrunedAfterExpiry` / `TestLimiter_ExpiredKeyRemovedOnNextAllow` を追加。枯渇→期限切れのキー削除を保証。

### 直近のコミット(已反映)
- `a42e880` Fix stage reset cleanup — リセット時 `endingImage.onerror` 解除 + `src` 属性削除 + `gameEpoch` で遅延応答無効化
- `b13700e` Add stage reset button — stage 1〜4 に確認ダイアログ付き「初めからやり直す」ボタン(mic左)
- `1ebd64f` Document concurrency limits
- `ed7e8a6` Adjust stage layout proportions — `stage-content` で画像75% / 説明25% 横並び
- `d44e82d` docs: .ai-handoff.md更新
- `b725fad` fix: README画像参照をw.jpg統一 + マイクボタン角丸小(180×90px)
- `0710447` change: jpg to 16:10 — ファイル名 `w` サフィックス(`s1w.jpg` 等)
- `ef75887` feat: エンディングに冒険履歴ベースの約300〜400字物語生成

### 動作確認済み(2026-07-07・修正後)
- `go test ./...`: 全緑(ratelimit 6件・handler含む全パッケージ)
- `npm run build`: 成功(28.52 KB)
- `gofmt -s`: クリーン
- `go vet ./...`: 警告なし

## 進行中の状態

- **ブランチ**: `main`(未プッシュ・`origin/main` より先行)
- **未コミット変更**: あり(H1-H5 の修正・handoff 更新)
- **APIキー**: `2026/.env` に設定済み(`source ~/.glm` で読込)
- **画像**: 全て `w.jpg`(16:10)。`web/public/images/` に s1w-s4w/successfulw/failedw/practicew.jpg。古い `w` なしファイルも残存(コードから未参照・実害なし)
- **判定/ストーリー**: `gemini-3.1-flash-lite` で動作確認済み
- **画像生成**: 完全削除。固定JPGのみ
- **マイクボタン**: 角丸小サイズ(180×90px)
- **[高]5件**: 修正済み。**[中]12件・[低]多数**: 未対応(下記ステップ2・3参照)

### API課金見積もり(100プレイ時)
- gemini-3.1-flash-lite: 入力$0.25/100万・出力$1.50/100万
- 100プレイ: 入力40〜60万・出力8〜15万トークン → **約$0.16〜0.30**

## 次回の具体的なステップ

### 1. コミットのプッシュ(最優先)
現在の未コミット変更(H1-H5修正)をコミット済みなら `git push origin main` でリモートへ反映。

### 2. 実機UXの最終確認
`cd 2026 && make build && make run` → `http://localhost:8080/` で以下を検証(特に H1/H3/H4/H5 が展示条件で問題ないか):
- **H1検証**: 同ステージで Bad 連打→16手超→エンディング物語に**最新**の発言が含まれるか(古い発言ではないか)
- **H3検証**: マイク認識中に5分経過(タイムアップ)→「じかんが きたよ!」が出るか。マイク認識中のタイムアップで判定 API が飛ばないか
- **H4検証**: 新規ゲーム開始→数秒経過→リロード→残り秒数が正しく復元されるか(300秒に戻らないか)
- **H5検証**: 長時間稼働でメモリ使用量が安定するか(`ratelimit` の windows map が肥大化しない)
- 「初めからやり直す」ボタン・確認ダイアログ・リセット動作
- 4ステージ進行・エンディング物語(約300〜400字)
- メール送信で冒険物語+画像URL+署名

### 3. 重要度[中]の修正(展示前に対処推奨)

| 内容 | 場所 |
|------|------|
| `catalog.go` の `いるます` typo(LLM プロンプトに埋め込まれる) | `domain/catalog.go:9` |
| Gemini SDK 呼び出しに `context.WithTimeout` なし | `client.go:50`, `story_generator.go:29` |
| フロント fetch に `AbortController` なし(LLM 判定で数分固まる) | `web/src/infra/fetchApi.ts:12-24` |
| `showReason` の setTimeout 2連鎖が管理下にない | `main.ts:568-573` |
| `onJudge` の setTimeout コールバックに `gameEpoch` チェックなし | `main.ts:685-706` |
| `setLang` で `saveSession` 呼び忘れ(リロードで言語戻る) | `main.ts:276` |
| `crypto/rand` 失敗で `panic`(プロセス全体クラッシュ) | `sysid.go:14-21` |
| `sanitizeReason` の regexp を package-level へ(毎回コンパイル) | `judge.go:103` |
| `viewport` の `user-scalable=no` は WCAG 1.4.4 違反 | `web/index.html:5` |
| `judgePractice` の成功/失敗語が ja/en ハードコード | `main.ts:441-450` |
| `state.ts` の `start()` がデッドエクスポート | `state.ts:46-48` |

### 4. 重要度[低]のリファクタ(展示後でも可)

- `main.ts`(972行)の分割: `session.ts`/`i18n.ts`/`practice.ts`/`resultView.ts`
- `buildAdventureBody`(`main.ts:832`)と `/r/{id}` 版(`main.ts:943`)の重複解消
- 履歴フォーマット3通り(`main.ts:212,577,843`)の統一。`slice(0,30)`/`slice(0,40)` の定数化
- `handler.go:92` の `maxHistory=16` と `ending.go:28` の `endingHistoryLimit=16` の重複解消
- gemini adapter の純粋関数(`firstText`, route フォールバック)の単体テスト追加(現在カバレッジ0%)
- `StoryGenerator` が interface(`ports.go:43`)と struct(`story_generator.go:14`)で同名・混同の元
- 未使用 i18n キー削除: `hud.livesEmpty`, `intro.pickLang`, `ending.fallbackStory`, `ending.netErrorStory`
- CSS: `@media` クエリ追加、hex 変数化(`#c85a3e`, `#4caf50` 等)、`img[src$=".jpg"]` と `.stage-image`/`.ending-image` の競合解消
- `web/public/sherpa/{ja,en}`(約370MB)が `src/` から参照ゼロ・デッドアセット確認
- `vite.config.ts:13-14` の `/img` プロキシがデッド設定
- 動的更新領域に `aria-live` 付与(`judge-message`/`judge-reason`/`history`/`ending-result`)

### 5. 本番デプロイ
- 公開Linuxサーバへ `2026/` 配置 → `make build` / `make build-bin`
- `2026/.env` の `PUBLIC_BASE_URL` 設定
- nginx で HTTPS → `:8080`、systemd で常駐
- `static/images/` に `w.jpg` 群を配置

## よく使うコマンド

```bash
cd 2026
make                  # build + unit(既定・ネットワーク不要)
make run              # サーバ起動(2026/.env を読む)
make check            # Clean Architecture チェック
cd web && npm run build  # フロントのみビルド
```

## 設計上の判断点(記録)
- **4ステージ構成**: stage1=川、stage2=ゴーレム、stage3=炎、stage4=ドラゴン。`isLast`判定は `stageIndex >= 3`
- **れんしゅうモード**: 別Phase("practice")。フロント簡易キーワード判定でサーバー不要。否定形は失敗に倒す
- **画像は固定JPG**(LLM生成完全廃止)。16:10アスペクト比。`web/public/images/*w.jpg` に版管理
- **メールは `mailto:`**(画像添付不可)。本文に画像URLをテキストで記載。base URLは `window.__FD_IMAGE_BASE__` でコンフィグ可能
- **エンディング物語**: Gemini に `state.history`(冒険履歴)を渡し、プレイヤーの実際の発言を組み込んだ約300〜400字の物語を生成
- **エンディングメッセージ**: クリア時「けっか: クリア!」、ライフ0時「けっか: ざんねん!」、タイムアップ時「じかんが きたよ!」で区別
- **reason のライフ言及は `sanitizeReason`(`usecase/judge.go`)で最終防御**
- **UXは「行動」ベース**(「呪文」呼称は全廃)
- **エンディング到達時はマイクdisabled**。「しゅうりょう」ラベルで明示
- **表示領域は80vw**(上限1200px)
- **セキュリティ**: ボディ上限64KiB、History配列上限16件/各300ルーン、`crypto/rand`失敗時はフォールバック廃止
- 来年は `2026/` を `2027/` にコピーし、`domain/catalog.go` + `web/src/app/messages.ts` + `web/public/images/` を差し替えれば再利用可能
