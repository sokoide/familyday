# AI Handoff — familyday(ドラゴン城の秘宝 2026)

最終更新: 2026-07-05
現在のブランチ: `main`(未プッシュ・origin/main より先行)

## 完了したこと

### feat: エンディングに「これまでの ぼうけん」を追加
- エンディング画面に `これまでの ぼうけん` を追加し、各ステージの発言と判定結果を表示
- `state.history` を一覧表示して、メール本文の冒険記録と同じ内容を見せるようにした

### fix: 画像URLを jpg に統一
- ステージ画像、エンディング画像、メール本文の画像URLを `.jpg` に統一
- `s1-s4 / successful / failed / practice` の参照を `.jpg` に修正

### fix: メールURLとPUBLIC_BASE_URLの連携
- `mailto:` の本文エンコードを `URLSearchParams` 依存から外し、空白が `+` にならないよう修正
- `PUBLIC_BASE_URL` を `index.html` に注入して、メール本文の画像URLが環境に追従するよう修正
- フロントの画像baseは `window.__FD_IMAGE_BASE__` 優先、未指定時は現在の origin の `/images` を使う

### fix: 画像生成Gemini Callの完全削除
- `/api/ending` から Gemini 画像生成を削除し、成功/失敗画像は静的 JPG のみを使用
- `ImageGenerator` ポート/実装/設定/テストを削除
- エンディング応答の `imageUrl` は `successful.jpg` / `failed.jpg` を返す

### feat: エンディング要約をGeminiで生成
- `/api/ending` で冒険履歴(`state.history`)とユーザー発言をまとめて Gemini に渡し、1〜2文の要約ストーリーを生成
- エンディング画面にストーリー本文を表示し、メール本文にも同じ要約を添付
- `/r/{id}` の結果復元画面でも要約を表示

### `5c69a89` fix: 練習モードのGUI整理
- practice-message/practice-result を story-card 内に移動（余分な白い枠を削除）
- practice-result に hidden 属性を追加し、空枠が表示されないよう制御
- initPractice/submitPractice/エラー時の hidden 制御を整理

### `323733e` feat: れんしゅうモード追加 + 画像をjpgに統一
- **れんしゅうモード**: intro画面に「れんしゅう」ボタン(黄色)を追加。practice画面で practice.jpg とかばんのおとしもの状況を表示。交番に届ける→成功(緑)、無視して立ち去る→失敗(赤)。フロント簡易キーワード判定(サーバー不要・即時・日英対応)
- **画像をjpgに統一**: s1-s4/successful/failed/practice を png→jpg に差し替え

### `bcfad36` style: 表示領域を80vwに拡大
### `76024b5` feat: 4ステージ化・エンディングUX改善・メール充実・音声認識修正
- 4ステージ構成(stage1=川→stage4=ドラゴン)
- エンディング画面をstory-card内に統一、成功/失敗メッセージ(色付き)
- エンディング到達時マイクを「しゅうりょう」でdisabled
- メール本文に冒険記録+画像URL+署名追加
- 固定画像表示(LLM生成廃止)
- 音声認識UX改善(interim採用・判定中表示・reasonネガティブ理由)

## 進行中の状態

- **ブランチ**: `main`(未プッシュ)
- **APIキー**: `2026/.env` に設定済み(`source ~/.glm` で読込)
- **画像**: 全て `.jpg` 形式。`web/public/images/` に s1-s4/successful/failed/practice.jpg を配置
- **判定/ストーリー**: `gemini-3.1-flash-lite` で動作確認済み
- **画像生成**: 画像作成のGemini Callは削除済み。成功/失敗は固定JPGのみ
- **ストーリー生成**: 冒険履歴を材料に Gemini でエンディング要約を生成し、画面とメールに反映
- **UI**: イントロに「れんしゅう」/「ぼうけんを はじめる」の2メニュー。エンディングで履歴一覧を表示済み

### 動作確認済み(2026-07-05)
- `npm run build`: TSビルド成功(26KB)
- `go test ./...`: 全パッケージ緑
- 画像配信: practice.jpg含む全画像 HTTP 200

## 次回の具体的なステップ

### 1. 実機UXの最終確認
- `make run` → `http://localhost:8080/` で以下を確認:
  - intro画面に「ぼうけんをはじめる」と「れんしゅう」の2ボタンが表示されるか
  - れんしゅう: practice.jpgが表示され、「こうばんにとどける」で成功(緑)、「むしする」で失敗(赤)になるか
  - れんしゅう画面に余分な白い枠やマイクアイコンが重複していないか
  - 4ステージ(stage1=川→stage4=ドラゴン)が正しく進行するか
  - エンディング画像がstory-card内に表示され、成功/失敗メッセージが色付きで出るか
  - エンディングで「これまでの ぼうけん」が表示されるか
  - エンディング到達時マイクが「しゅうりょう」で押せないか
  - メール送信で冒険記録+画像URL+署名が入るか
  - 表示領域がウィンドウ幅80%になっているか

### 2. 音声入力の確定
- macOS Edge で `[speech] error:` のコードをコンソール確認
- 会場は Windows 11 Edge。**Windows 実機でのマイク動作確認が必須**

### 3. 本番デプロイ
- 公開Linuxサーバへ `2026/` 配置 → `make build` / `make build-bin`
- `2026/.env` の `PUBLIC_BASE_URL` を設定
- nginx 等で HTTPS → `:8080`。systemd で常駐

### 4. コミットのプッシュ
- `git push origin main`

## よく使うコマンド

```bash
cd 2026
make                  # build + unit(既定・ネットワーク不要)
make run              # サーバ起動(2026/.env を読む)
make check            # Clean Architecture チェック
cd web && npm run build  # フロントのみビルド
```

## 設計上の判断点(記録)
- **4ステージ構成**: stage1=川、stage2=ゴーレム、stage3=炎、stage4=ドラゴン。`isLast`判定は `stageIndex >= 3`
- **れんしゅうモード**: 別Phase("practice")。フロント簡易キーワード判定でサーバー不要。1ステージのみ。メッセージ・結果はstory-card内に統合
- **画像は固定JPG**(LLM生成廃止)。`web/public/images/` に版管理。来年は同名上書きで差し替え
- **メールは `mailto:`**(画像添付不可)。本文に画像URLをテキストで記載。base URLは `window.__FD_IMAGE_BASE__` でコンフィグ可能(デフォルトは現在の origin の `/images`)
- **エンディング要約**: Gemini に `state.history` を渡して、エンディング画面・メール・復元画面に同一ストーリーを出す
- **reason のライフ言及は `sanitizeReason`(`usecase/judge.go`)で最終防御**
- **UXは「行動」ベース**(「呪文」呼称は全廃)
- **エンディング到達時はマイクdisabled**。「しゅうりょう」ラベルで明示
- **表示領域は80vw**(上限1200px)
- 来年は `2026/` を `2027/` にコピーし、`domain/catalog.go` + `web/src/app/messages.ts` + `web/public/images/` を差し替えれば再利用可能
