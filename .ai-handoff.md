# AI Handoff — familyday(ドラゴン城の秘宝 2026)

最終更新: 2026-07-05
現在のブランチ: `main`(未プッシュ・origin/main より先行)

## 完了したこと

### `2c7d8f5` fix: レビュー残り3件対応(History上限・タイムアップ統一・README)
- **handler.go**: History配列に要素数上限(16件)・各フィールド長上限(300ルーン)を追加。`truncateRunes` ヘルパーで過剰データを切り詰め、プロンプト膨張DoSを抑止
- **main.ts**: btn-start のタイムアップ onZero に `clearPendingAdvanceTimer` + mic/manual disabled + ended ラベル設定を追加。セッション復元側と統一
- **README.md**: 状態遷移mermaidにStage4とPracticeを追加(4ステージ+練習)、練習モードセクションを新設(キーワード判定・否定形処理・画像を記載)

### `d23cf76` Remove obsolete image generation paths
- 画像生成の Gemini Call を完全削除、成功/失敗は固定 JPG のみを使用
- `Image` ポート、`/img/` ルート、`generated/` 保存、`safeFileServer` の旧経路を削除
- エンディング永続化はメタ JSON のみ保存に単純化
- README/.env.example から画像生成系env・記述を削除、固定JPG配信仕様に書き直し
- `.env.example` から `GEMINI_MODEL_IMAGE/SIZE/COUNT` を削除
- `sysid.go`: crypto/rand 失敗時の予測可能ID fallbackを廃止
- `handler.go`: リクエストボディ上限(`MaxBytesReader` 64KiB)を追加、超過時413
- `main.ts`: state.phase遷移修正(`start()`呼出)、練習モード否定形誤判定修正、setTimeout競合修正(`pendingAdvanceTimer`管理)、練習もどるで`speech.stop()`
- `ending.go`: ディスク書込失敗を500系に分類
- 統合テスト・未使用依存を整理(`firstInlineImage`削除、`go mod tidy`で`golang.org/x/image`除去)

### `3e45a56` docs: make build-bin の本番設置方法を README に追記
### `5682ce1` fix: integration の imageUrl 期待値を固定画像へ更新
### `8a54e83` feat: エンディング要約をGeminiで生成(state.historyから)

### 旧コミット（参考）
- `5c69a89` 練習モードGUI整理（story-card内に統合）
- `323733e` れんしゅうモード追加 + 画像をjpgに統一
- `bcfad36` 表示領域を80vwに拡大
- `76024b5` 4ステージ化・エンディングUX改善・メール充実・音声認識修正

## 進行中の状態

- **ブランチ**: `main`(未プッシュ)
- **APIキー**: `2026/.env` に設定済み(`source ~/.glm` で読込)
- **画像**: 全て `.jpg`。`web/public/images/` に s1-s4/successful/failed/practice.jpg を配置
- **判定/ストーリー**: `gemini-3.1-flash-lite` で動作確認済み
- **画像生成**: 完全削除済み。固定JPGのみ使用
- **ストーリー生成**: 冒険履歴を材料に Gemini でエンディング要約を生成

### 動作確認済み(2026-07-05)
- `go build` / `go vet` / `go test ./...`: 全緑
- `gofmt -s`: クリーン
- `npm run build`: 成功(27.9KB)
- `go test -tags=integration`: 実Gemini API で成功確認済み

### レビュー指摘の対応状況
- **P0全件解消**: state.phase遷移、練習否定形誤判定、.env.example画像生成env削除
- **P1全件解消**: ボディ上限(64KiB)、History配列上限(16件/300ルーン)、setTimeout競合、タイムアップ時マイクdisabled統一、練習もどる音声停止、README書き直し
- **次回対応(P2)**: storyPrompt構造化、clearSession try/catch、integration_test コメント更新

## 次回の具体的なステップ

### 1. 実機UXの最終確認
- `make run` → `http://localhost:8080/` で以下を確認:
  - intro画面に「ぼうけんをはじめる」と「れんしゅう」の2ボタン
  - れんしゅう: practice.jpg表示、「こうばんにとどける」→成功(緑)、「とどけない」→失敗(赤)
  - 4ステージ(stage1=川→stage4=ドラゴン)が正しく進行
  - リロードで進行が復元される（state.phase修正の確認）
  - タイムアップ時マイクが「しゅうりょう」でdisabled（両onZero統一の確認）
  - エンディングで「これまでの ぼうけん」が表示
  - エンディング到達時マイクが「しゅうりょう」で押せない
  - メール送信で冒険記録+画像URL+署名が入る
  - 表示領域がウィンドウ幅80%

### 2. 音声入力の確定
- macOS Edge で `[speech] error:` のコードをコンソール確認
- 会場は Windows 11 Edge。**Windows 実機でのマイク動作確認が必須**

### 3. 本番デプロイ
- 公開Linuxサーバへ `2026/` 配置 → `make build` / `make build-bin`
- `2026/.env` の `PUBLIC_BASE_URL` を設定
- nginx 等で HTTPS → `:8080`。systemd で常駐
- `static/images/` 配置を忘れずに（画像が全部404になる）

### 4. コミットのプッシュ
- `git push origin main`

## よく使うコマンド

```bash
cd 2026
make                  # build + unit(既定・ネットワーク不要)
make run              # サーバ起動(2026/.env を読む)
make check            # Clean Architecture チェック
cd web && npm run build  # フロントのみビルド
```

## 設計上の判断点(記録)
- **4ステージ構成**: stage1=川、stage2=ゴーレム、stage3=炎、stage4=ドラゴン。`isLast`判定は `stageIndex >= 3`
- **れんしゅうモード**: 別Phase("practice")。フロント簡易キーワード判定でサーバー不要。否定形は失敗に倒す。1ステージのみ
- **画像は固定JPG**(LLM生成完全廃止)。`web/public/images/` に版管理。来年は同名上書きで差し替え
- **メールは `mailto:`**(画像添付不可)。本文に画像URLをテキストで記載。base URLは `window.__FD_IMAGE_BASE__` でコンフィグ可能(デフォルトは現在の origin の `/images`)
- **エンディング要約**: Gemini に `state.history` を渡して、エンディング画面・メール・復元画面に同一ストーリーを出す
- **reason のライフ言及は `sanitizeReason`(`usecase/judge.go`)で最終防御**
- **UXは「行動」ベース**(「呪文」呼称は全廃)
- **エンディング到達時はマイクdisabled**。「しゅうりょう」ラベルで明示（タイムアップ含む全経路で統一）
- **表示領域は80vw**(上限1200px)
- **セキュリティ**: ボディ上限64KiB、History配列上限16件/各300ルーン、`crypto/rand`失敗時はフォールバック廃止
- 来年は `2026/` を `2027/` にコピーし、`domain/catalog.go` + `web/src/app/messages.ts` + `web/public/images/` を差し替えれば再利用可能
