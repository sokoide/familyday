# AI Handoff — Family Day 2026

## 完了したこと(今回)

残っていたレビュー指摘2件(高)を修正・検証済み。

### 1. レートリミッタのメモリリーク(サーバ)
- `server/internal/adapters/infra/ratelimit/rate_limiter.go`
  - `Limiter` に `lastSweep time.Time` を追加。
  - `Allow` 冒頭で「前回の全キー掃除から 60s 以上経過」なら `sweepLocked()` が全キーを走査し、期限内ヒット0件の dead key を `delete`。goroutine 不要・`now` 注入で決定的検証可能。
  - 自キープルーニング(既存)はホットキー即時解放用に残置。
- `server/internal/adapters/infra/ratelimit/rate_limiter_test.go`
  - `TestLimiter_DeadKeysSweptPeriodically` 追加(50 dead key → +61s → 別 key Allow で残1件)。
  - `TestLimiter_ExpiredKeyRemovedOnNextAllow` から漏れ自認コメントを削除。

### 2. タイムアウト後の遅延判定レスポンス(Web)
- `web/src/app/main.ts` の `submitInput`(`await judgeApi.judge` 後、成功/catch 両ブランチ)
  - ガードを `epoch !== gameEpoch` → `epoch !== gameEpoch || state.phase !== "stage"` に強化。
  - タイムアウトで ending 遷移後も gameEpoch が更新されず、遅延判定が履歴重複・`goEnding()` 再実行を引き起こすのを防止。

## 検証
- `cd server && go test ./...` → 緑
- `cd web && npm run build` → 緑
- 修正2の実機手動検証(タイムアウト直後の遅延判定で重複なし)は未実施 → 推奨。

## 進行中の状態
- コミット済み(本ハンドoff更新と同時に commit 実行)。
- ワーキングツリーは clean を想定。

## 次回の具体的ステップ
1. (推奨)修正2の実機検証。タイマーを短くするか判定 API を意図的に遅延させ、タイムアウト直後に判定レスポンスが来ても: 履歴が重複しない / ending 通信が2重発火しない / state.stageIndex が ending 中に書き換わらないことをブラウザ Network パネル + 画面で確認。
2. 他に残件があればレビュー観点で拾う。現時点で既知の高/中指摘はなし。
